# Astro + Cloudflare dependency optimization issues repro

`pnpm dev` => Navigate to http://localhost:4321/

Run `pnpm clean` to start a fresh repro. Deletes `./node_modules/.vite/`, `/.wrangler/` and `./.astro/` folders.
